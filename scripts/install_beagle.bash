# Add can to network interfaces
#echo "" >> /etc/network/interface
#echo "" >> /etc/network/interface
#echo "" >> /etc/network/interface

echo "Install beagle bone overlays to provide tools for pins"
sudo apt update
sudo apt install bb-cape-overlays